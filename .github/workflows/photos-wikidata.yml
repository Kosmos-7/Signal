# .github/workflows/photos-wikidata.yml
# Photos de sociétés par correspondance d'IDENTITÉ (Wikidata) plutôt que par
# recherche de mots. Voir tools/photos_wikidata.py pour le raisonnement.
name: Photos de sociétés via Wikidata

on:
  workflow_dispatch:
    inputs:
      telecharger:
        description: "Télécharger les images en plus du rapport"
        type: boolean
        default: false
      limite:
        description: "Ne traiter que les N premières sociétés (0 = toutes)"
        type: string
        default: "0"
      depuis_rapport:
        description: "Réutiliser photos_wikidata.json au lieu de réinterroger Wikidata"
        type: boolean
        default: false

permissions:
  contents: write

concurrency:
  group: signal-data-commit
  cancel-in-progress: false

jobs:
  photos:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Install Pillow
        run: pip install --quiet Pillow
      - name: Interroger Wikidata
        # `limite` est `type: string` : elle porte du texte libre qu'un shell
        # exécuterait, elle passe donc par l'environnement (règle de
        # photos-marques.yml). Les deux drapeaux ci-dessous sont des booléens qui
        # rendent un littéral figé — '--telecharger' ou rien — et ne peuvent
        # porter aucune entrée : ils restent interpolés sans risque.
        env:
          LIMITE: ${{ inputs.limite }}
        run: |
          python tools/photos_wikidata.py --limite "${LIMITE:-0}" \
            ${{ inputs.telecharger && '--telecharger' || '' }} \
            ${{ inputs.depuis_rapport && '--depuis-rapport' || '' }}
      - name: Publier le rapport
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: photos-wikidata
          path: |
            photos_wikidata.json
            assets/titres/candidats/
          retention-days: 7
      - name: Commiter pour arbitrage visuel
        run: |
          git config user.email "bot@signal.fr"
          git config user.name "Signal Bot"
          # Un chemin inexistant fait echouer TOUT le git add, y compris les
          # chemins valides qui l'accompagnent : si le script meurt avant
          # d'ecrire son rapport, les images telechargees sont perdues.
          git add -A assets/ || true
          git add -A photos_wikidata.json || true
          if git diff --staged --quiet; then
            echo "Rien de nouveau."
          else
            git commit -m "chore(photos): rapport Wikidata + candidats [skip ci]"
            git pull --rebase --autostash || { git rebase --abort; exit 1; }
            git push
          fi
